# RegNeps.Mobile

Cliente Android .NET MAUI para `RegNeps.Web`.

La app abre el servidor RegNeps en un WebView y permite cambiar la URL del servidor desde el dispositivo. Para instrucciones de compilación e instalación consulta `docs/ANDROID_APK.md`.
